_schema-version: '3.1'
ID: COLLABORATIVE_CONTRACT_TERM
version: 1.0.0
description: "A simple CAP project."
parameters:
  enable-parallel-deployments: true
build-parameters:
  before-all:
    - builder: custom
      commands:
        - npx cds build --production
modules:
  - name: COLLABORATIVE_CONTRACT_TERM-srv
    type: nodejs
    path: gen/srv
    parameters:
      buildpack: nodejs_buildpack
    build-parameters:
      builder: npm
    provides:
      - name: srv-api # required by consumers of CAP services (e.g. approuter)
        properties:
          srv-url: ${default-url}
    requires:
      - name: COLLABORATIVE_CONTRACT_TERM-db
      - name: COLLABORATIVE_CONTRACT_TERM-destination
      - name: COLLABORATIVE_CONTRACT_TERM-connectivity

  - name: COLLABORATIVE_CONTRACT_TERM-db-deployer
    type: hdb
    path: gen/db
    parameters:
      buildpack: nodejs_buildpack
    requires:
      - name: COLLABORATIVE_CONTRACT_TERM-db

resources:
  - name: COLLABORATIVE_CONTRACT_TERM-db
    type: com.sap.xs.hdi-container
    parameters:
      service: hana
      service-plan: hdi-shared
  - name: COLLABORATIVE_CONTRACT_TERM-destination
    type: org.cloudfoundry.managed-service
    parameters:
      service: destination
      service-plan: lite
  - name: COLLABORATIVE_CONTRACT_TERM-connectivity
    type: org.cloudfoundry.managed-service
    parameters:
      service: connectivity
      service-plan: lite
